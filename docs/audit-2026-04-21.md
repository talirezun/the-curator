# Codebase Audit — AI Wiki Health roadmap

**Date:** 2026-04-21
**Version on landing:** 2.4.0 (commit pending approval before push)
**Auditor:** Claude Opus 4.7 (1M context)
**Scope:** Phase 0 of the AI-assisted Wiki Health roadmap (see previous audit [`audit-2026-04-20.md`](audit-2026-04-20.md) for v2.3.x work). This document covers work performed 2026-04-21 onward.

---

## Phase 0 — Model-lifecycle safety net (v2.4.0)

### Motivation

Every LLM call in the Curator flows through `generateText()` in [`src/brain/llm.js`](../src/brain/llm.js). The pinned default model IDs (`gemini-2.5-flash-lite`, `claude-sonnet-4-6`) are provider-controlled strings: when a provider retires or renames one of them, every Curator installation pinned to that ID starts returning 404 and the core features (ingest, chat, upcoming AI-Health) fail silently for non-technical users.

This audit phase builds a safety net so existing installations survive provider deprecations until the next Check-for-Updates cycle.

### Scope audit (files read, no changes)

Files touched by Phase 0:
- `src/brain/llm.js` — adds fallback chain logic
- `src/routes/config.js` — one new field in `/api/config/api-keys` response
- `src/public/app.js` — one new function `renderFallbackBanner()`
- `src/public/styles.css` — one new class `.settings-fallback-banner`

Files audited but NOT modified:
- `src/brain/ingest.js` — calls `generateText()` with `'json'` response format. Confirmed that JSON prompting works on both Gemini (native `responseMimeType`) and Anthropic (prompt instruction + `parseJSON` with `jsonrepair`). Fallback chain is transparent to callers.
- `src/brain/chat.js`, `src/brain/query.js` — text-mode `generateText()` callers. No changes needed.
- `src/brain/files.js`, `src/brain/sync.js`, `src/brain/health.js`, `src/brain/config.js` — no LLM calls; untouched.
- `mcp/` — read-only; doesn't use `llm.js`.
- `src/routes/*.js` (excluding config.js) — unchanged.

This means: ingest, chat, health (algorithmic fixes), sync, MCP, updater, installer — none of their code paths are modified. Only the common LLM dispatch layer gains a fallback wrapper.

### Design decisions

**1. Fallback only on `model-not-found`.** Rate limits (429) and service-unavailable (503) are NOT retried via the fallback chain — those already have dedicated exponential-backoff handling in the existing `generateText()` retry loop. Auth errors (401) and permission errors (403) propagate immediately as before. This keeps the chain's semantics crisp: "the pinned model no longer exists."

**2. Error detection via message + status matching.** Anthropic throws a typed `NotFoundError` with `status: 404`; Gemini returns generic errors with messages like `"[404 Not Found] models/X is not found for API version Y"`. The `isModelNotFound()` helper matches both via a combination of status code, the literal `"404"` substring + `"not found"`/`"is not supported"` keywords, and the Anthropic-specific `"not_found_error"` marker. Tested successfully against the Gemini case; Anthropic case covered by the typed `status === 404` short-circuit.

**3. Module-level fallback state, not per-request.** A single `_activeFallback` variable tracks the most recent fallback event. It's set when a fallback succeeds, cleared when the primary succeeds again. This is per-process (lost on restart) which is correct — on restart, the server retries the primary first and the state re-establishes itself from reality.

**4. Gemini and Anthropic chains are separate.** Different generations of models with different pricing/capability profiles; mixing them would make "Active model" unreadable.

**5. Settings UI surfaces fallback prominently but unobtrusively.** An amber banner below the provider badge, text tells the user what's happening and where to click to update. No modal, no blocking — the app still works, the banner is information + call-to-action.

### Implementation summary

```js
// llm.js additions (strictly additive — existing callLLM refactored to callProvider + fallback loop)
const FALLBACK_CHAINS = {
  gemini: ['gemini-2.5-flash', 'gemini-1.5-flash', 'gemini-1.5-flash-latest'],
  anthropic: ['claude-sonnet-4-5', 'claude-3-7-sonnet-latest', 'claude-3-5-sonnet-latest'],
};
let _activeFallback = null;
export function getFallbackStatus() { return _activeFallback; }
function isModelNotFound(err) { /* status 404 + message matching */ }
async function callProvider(provider, model, ...) { /* was the body of callLLM */ }
async function callLLM(...) {
  const chain = [primary, ...FALLBACK_CHAINS[provider]];
  for (let i = 0; i < chain.length; i++) {
    try {
      const result = await callProvider(provider, chain[i], ...);
      if (i === 0) clearFallbackForProvider(provider);
      else setFallback(chain[0], chain[i]);
      return result;
    } catch (err) {
      if (isModelNotFound(err) && i < chain.length - 1) continue;
      throw err;
    }
  }
}
```

Config endpoint: `/api/config/api-keys` now includes `fallback: getFallbackStatus()`. Frontend reads it and renders an amber banner if populated.

### Test matrix (performed locally on computer 1 before any commit)

| Test | Expected | Result |
|---|---|---|
| Baseline: primary Gemini call (chat) | 200, answer, no fallback | ✅ `answer: "Yes, this wiki is designed to be a dedicated second brain..."` |
| `/api/config/api-keys` after baseline | `fallback: null` | ✅ |
| `LLM_MODEL=gemini-deprecated-nonexistent` override → chat | Fallback fires, answer returned | ✅ `answer: "FALLBACK OK"` |
| Server log during simulated deprecation | Two `[llm]` warnings (not-found + using-fallback) | ✅ Both present with clean text |
| `/api/config/api-keys` after fallback | `fallback: {provider, requestedModel, usingModel, at}` populated | ✅ All four fields correct |
| Ingest a tiny `.txt` with fallback active | 5 pages written, SSE stream completes cleanly | ✅ `pagesWritten: ["summaries/mini-fallback-test.md", 4 concepts]` |
| MCP self-test with fallback active | `ok: true`, 10 tools, all domains | ✅ |
| Health scan with fallback active | 597 brokenLinks, 0 missingBacklinks (v2.3.9 baseline) | ✅ |
| Sync status with fallback active | unchanged, returns current repo state | ✅ |
| Restart WITHOUT override → primary call | Primary succeeds, `fallback: null` | ✅ |
| Restart WITHOUT override → then any feature | No warnings in log, no UI banner | ✅ |
| Update-check endpoint | `current: 2.3.9, updateAvailable: false` (pre-bump) | ✅ |

### Anthropic pipeline verification

The user reports never having tested Anthropic in production. The code path was statically audited:

1. **Model ID in `DEFAULTS`:** `claude-sonnet-4-6`. The bundled SDK v0.39.0 `Model` type lists up to `claude-3-7-sonnet-latest` explicitly, but accepts arbitrary strings via `(string & {})` — so the newer ID will pass build and runtime *if Anthropic's API accepts it*. If it 404s, the fallback chain catches it with `claude-sonnet-4-5` → `claude-3-7-sonnet-latest` → `claude-3-5-sonnet-latest`.
2. **JSON response handling:** Anthropic has no `responseMimeType` equivalent. The `ingest.js` prompts instruct *"Return ONLY valid JSON"* explicitly; downstream, `parseJSON()` with `jsonrepair` absorbs minor deviations. Functionally equivalent but less deterministic than Gemini's native JSON mode.
3. **Error detection:** Anthropic SDK throws typed `NotFoundError` with `status: 404`. The `isModelNotFound()` helper catches this via the explicit `err.status === 404` branch.

Result: Anthropic pipeline is structurally sound; the one caveat is that JSON-producing calls (ingest) may occasionally require the `jsonrepair` safety net more often than with Gemini. This is noted in `docs/model-lifecycle.md`.

### Deliverables

- [`src/brain/llm.js`](../src/brain/llm.js) — fallback chain, `getFallbackStatus()`, `isModelNotFound()`
- [`src/routes/config.js`](../src/routes/config.js) — `/api/config/api-keys` grows a `fallback` field
- [`src/public/app.js`](../src/public/app.js) — `renderFallbackBanner()` attached to `loadApiKeyStatus()`
- [`src/public/styles.css`](../src/public/styles.css) — `.settings-fallback-banner` class
- [`docs/model-lifecycle.md`](model-lifecycle.md) — new user-facing guide
- [`CLAUDE.md`](../CLAUDE.md) — v2.4.0 row + active-decision entry
- [`package.json`](../package.json) — version 2.3.9 → 2.4.0

### Non-deliverables (explicitly out of scope for Phase 0)

- No AI-assisted Health fix endpoints (Phase 1+)
- No user-facing model picker in Settings (deferred; `LLM_MODEL` env var remains the developer escape hatch)
- No upgrade to newer Anthropic SDK version (noted as future work; v0.39.0 accepts string model IDs so functional)
- No changes to the ingest, chat, health-scan, health-fix, sync, MCP, updater, or installer code paths

### Rollback plan

The entire Phase 0 change set is one commit (v2.4.0). `git revert <sha> && git push` reverts cleanly because:
- No data format or schema changes
- No dependency additions
- No existing API endpoints modified (only field additions, which older clients ignore)
- No filesystem layout changes

### Next phase

Phase 1 — AI-suggest for broken links (`/api/health/:domain/ai-suggest`). Builds on top of v2.4.0's hardened LLM layer. Planned as a separate commit / version bump. Audited after Phase 1 work lands.

---

## Phase 0 Addendum — Anthropic low-cost default (v2.4.1)

### Motivation

v2.4.0 shipped with `DEFAULTS.anthropic = 'claude-sonnet-4-6'` — Sonnet being Anthropic's *mid-tier* model. For wiki ingestion of large libraries, this mis-matched the cost profile of the Gemini default (`gemini-2.5-flash-lite`, which is explicitly the low-cost tier). The user clarified that the Anthropic default should be the low-cost tier too.

### Change

- `DEFAULTS.anthropic`: `claude-sonnet-4-6` → **`claude-haiku-4-5`**
- `FALLBACK_CHAINS.anthropic` reordered to prefer Haiku variants first (same cost tier), then escalate to Sonnet family only if the entire Haiku line is retired:
  - `claude-3-5-haiku-latest` — previous Haiku gen, SDK-typed, definitely works
  - `claude-3-5-haiku-20241022` — explicit stable version as last-resort Haiku
  - `claude-sonnet-4-5` — quality upgrade tier
  - `claude-3-7-sonnet-latest` — rolling alias
  - `claude-3-5-sonnet-latest` — deep fallback

### Opt-out for users who want Sonnet

Documented in `docs/model-lifecycle.md`:

```bash
# in .env — if you want higher-quality Anthropic for dense content
LLM_MODEL=claude-sonnet-4-5
```

The fallback chain still applies on top of the override.

### Step 3 (fallback verification) completed on computer 1

Per user request, I executed the full Step 3 test matrix myself on computer 1. All checks passed:

| # | Action | Expected | Actual |
|---|---|---|---|
| 3a | Start with `LLM_MODEL=gemini-deprecated-nonexistent` | `activeModel` reports the bad name, `fallback: null` until first call | ✅ exactly as predicted |
| 3b | Trigger chat | Response arrives normally via fallback | ✅ answer: `"FALLBACK TEST SUCCESS"` |
| 3c | `/api/config/api-keys` after chat | `fallback: {provider, requestedModel, usingModel, at}` populated | ✅ `fallback.usingModel = "gemini-2.5-flash"` |
| 3d | Server log | Two `[llm]` warnings (not-found + using-fallback) | ✅ both present |
| 3e | Frontend code path | JS reads `data.fallback`, creates `#settings-model-fallback` element with class `settings-fallback-banner`, CSS defined | ✅ all three wiring points verified |
| 3f | Sync / Health / MCP under fallback | All succeed with identical output to baseline | ✅ sync status clean, health scan 597/0/794, MCP self-test ok + 10 tools |
| 3g | Restart without override | `fallback: null`, clean log, primary works | ✅ `answer: "Hey there! I've got the wiki ready..."` |

### Why ship as v2.4.1 rather than folding into v2.4.0

v2.4.0 was already pushed and verified by the user on computer 2. The Anthropic-default change is a **user-visible default** (changes cost profile) that warrants its own version row in the git-history table and deserves to be pulled separately so users on v2.4.0 can read the changelog before opting in.

### Deliverables

- [`src/brain/llm.js`](../src/brain/llm.js) — `DEFAULTS.anthropic` + `FALLBACK_CHAINS.anthropic` updated
- [`docs/model-lifecycle.md`](model-lifecycle.md) — Anthropic section reflects Haiku-first default + Sonnet opt-in
- [`CLAUDE.md`](../CLAUDE.md) — v2.4.1 row, version constant bumped to 2.4.1

---

## Phase 0 Addendum 2 — API-key UX fix (v2.4.2)

### Motivation

After v2.4.1 shipped, the user reported while testing provider switching on computer 2: *"which key has a priority because I cannot empty the field? … we should have the ability to disconnect from the connected LLM. … Nobody will use two same keys at the same time, this makes no sense."*

The v2.4.0/v2.4.1 behaviour had two concrete UX problems:

1. **No way to clear a stored key.** The Settings form's password inputs showed masked placeholders; typing nothing and clicking Save just kept the existing value (empty fields were filtered out of the POST body). Users had no control over which keys were stored.
2. **Hidden priority rule.** When both keys were stored, `getProviderInfo()` always preferred Gemini. To switch to Anthropic, a user had to open `.curator-config.json` in a text editor and manually delete the Gemini key — clearly not acceptable.

### Design

Two changes, both minimal and intuitive:

**A. Last-saved-wins.** Saving a non-empty key for a provider also marks that provider as active (`cfg.activeProvider`). Users don't juggle priority — they just paste the key they want to use. If a Gemini user wants to try Anthropic, they paste their Anthropic key and save. Done.

**B. Per-field Disconnect button.** For when you want to remove a key without adding another. Appears only when a key is stored. If the disconnected key was the active provider, active automatically moves to the other provider (if it has a key) or becomes null.

### Implementation

- [`src/brain/config.js`](../src/brain/config.js):
  - `setApiKeys({...})`: now also sets `cfg.activeProvider` to whichever non-empty key was just saved.
  - New `clearApiKey(provider)`: clears one provider's stored key and adjusts `activeProvider` if needed.
  - New `getActiveProvider()`: reads `cfg.activeProvider` with legacy fallback (Gemini-first-if-both) for configs written before v2.4.2.
- [`src/brain/llm.js`](../src/brain/llm.js):
  - `getProviderInfo()` uses `getActiveProvider()` instead of the hardcoded Gemini-first priority. A defensive fallback still exists if the active provider's key is somehow missing (e.g., manual config edit).
- [`src/routes/config.js`](../src/routes/config.js):
  - New endpoint `POST /api/config/api-keys/disconnect` with body `{provider}`.
- [`src/public/index.html`](../src/public/index.html) + [`src/public/app.js`](../src/public/app.js):
  - Each API-key field gains a small `Disconnect` button that appears only when the underlying key is stored.
  - `loadApiKeyStatus()` toggles the button's visibility from the `/api/config/api-keys` response.
  - A clickable confirmation prompt avoids accidental removal.
- [`src/public/styles.css`](../src/public/styles.css): `.key-field-footer`, `.key-disconnect-btn`, `.key-save-row`, `.key-save-hint` — minimal styling reusing existing design tokens.

### Backward compatibility

Existing `.curator-config.json` files don't have an `activeProvider` field. `getActiveProvider()` detects this and falls back to the v2.4.1 priority rule (whichever key exists; Gemini first if both), so existing users upgrading to v2.4.2 see zero behaviour change until they explicitly save a new key.

### Test matrix (computer 1)

Ran with a throwaway config to avoid touching the real keys:

| # | Action | Expected | Actual |
|---|---|---|---|
| 1 | Fresh config, no keys | `activeProvider: null`, badge hidden | ✅ |
| 2 | Save Gemini key | `activeProvider: gemini`, active model = `gemini-2.5-flash-lite` | ✅ |
| 3 | Save Anthropic key (both now stored) | `activeProvider: anthropic` (last-saved-wins), active model = `claude-haiku-4-5` | ✅ |
| 4 | Re-save Gemini with a different key | `activeProvider: gemini` again | ✅ |
| 5 | Disconnect Gemini (was active) | `activeProvider: anthropic` auto-switched (Anthropic key still stored) | ✅ |
| 6 | Disconnect Anthropic (the only remaining) | `activeProvider: null`, no active model | ✅ |
| 7 | Legacy config (no `activeProvider` field) + real chat | Works with Gemini-first fallback, `answer: "LEGACY COMPAT OK!"` | ✅ |

### Rollback

One commit, strictly additive at the config/routes/UI level. No data migration. Reverting restores v2.4.1 behaviour cleanly.
