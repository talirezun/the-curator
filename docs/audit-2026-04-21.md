# Codebase Audit — AI Wiki Health roadmap

**Date:** 2026-04-21
**Version on landing:** 2.4.0 (commit pending approval before push)
**Auditor:** Claude Opus 4.7 (1M context)
**Scope:** Phase 0 of the AI-assisted Wiki Health roadmap (see previous audit [`audit-2026-04-20.md`](audit-2026-04-20.md) for v2.3.x work). This document covers work performed 2026-04-21 onward.

---

## Phase 0 — Model-lifecycle safety net (v2.4.0)

### Motivation

Every LLM call in the Curator flows through `generateText()` in [`src/brain/llm.js`](../src/brain/llm.js). The pinned default model IDs (`gemini-2.5-flash-lite`, `claude-sonnet-4-6`) are provider-controlled strings: when a provider retires or renames one of them, every Curator installation pinned to that ID starts returning 404 and the core features (ingest, chat, upcoming AI-Health) fail silently for non-technical users.

This audit phase builds a safety net so existing installations survive provider deprecations until the next Check-for-Updates cycle.

### Scope audit (files read, no changes)

Files touched by Phase 0:
- `src/brain/llm.js` — adds fallback chain logic
- `src/routes/config.js` — one new field in `/api/config/api-keys` response
- `src/public/app.js` — one new function `renderFallbackBanner()`
- `src/public/styles.css` — one new class `.settings-fallback-banner`

Files audited but NOT modified:
- `src/brain/ingest.js` — calls `generateText()` with `'json'` response format. Confirmed that JSON prompting works on both Gemini (native `responseMimeType`) and Anthropic (prompt instruction + `parseJSON` with `jsonrepair`). Fallback chain is transparent to callers.
- `src/brain/chat.js`, `src/brain/query.js` — text-mode `generateText()` callers. No changes needed.
- `src/brain/files.js`, `src/brain/sync.js`, `src/brain/health.js`, `src/brain/config.js` — no LLM calls; untouched.
- `mcp/` — read-only; doesn't use `llm.js`.
- `src/routes/*.js` (excluding config.js) — unchanged.

This means: ingest, chat, health (algorithmic fixes), sync, MCP, updater, installer — none of their code paths are modified. Only the common LLM dispatch layer gains a fallback wrapper.

### Design decisions

**1. Fallback only on `model-not-found`.** Rate limits (429) and service-unavailable (503) are NOT retried via the fallback chain — those already have dedicated exponential-backoff handling in the existing `generateText()` retry loop. Auth errors (401) and permission errors (403) propagate immediately as before. This keeps the chain's semantics crisp: "the pinned model no longer exists."

**2. Error detection via message + status matching.** Anthropic throws a typed `NotFoundError` with `status: 404`; Gemini returns generic errors with messages like `"[404 Not Found] models/X is not found for API version Y"`. The `isModelNotFound()` helper matches both via a combination of status code, the literal `"404"` substring + `"not found"`/`"is not supported"` keywords, and the Anthropic-specific `"not_found_error"` marker. Tested successfully against the Gemini case; Anthropic case covered by the typed `status === 404` short-circuit.

**3. Module-level fallback state, not per-request.** A single `_activeFallback` variable tracks the most recent fallback event. It's set when a fallback succeeds, cleared when the primary succeeds again. This is per-process (lost on restart) which is correct — on restart, the server retries the primary first and the state re-establishes itself from reality.

**4. Gemini and Anthropic chains are separate.** Different generations of models with different pricing/capability profiles; mixing them would make "Active model" unreadable.

**5. Settings UI surfaces fallback prominently but unobtrusively.** An amber banner below the provider badge, text tells the user what's happening and where to click to update. No modal, no blocking — the app still works, the banner is information + call-to-action.

### Implementation summary

```js
// llm.js additions (strictly additive — existing callLLM refactored to callProvider + fallback loop)
const FALLBACK_CHAINS = {
  gemini: ['gemini-2.5-flash', 'gemini-1.5-flash', 'gemini-1.5-flash-latest'],
  anthropic: ['claude-sonnet-4-5', 'claude-3-7-sonnet-latest', 'claude-3-5-sonnet-latest'],
};
let _activeFallback = null;
export function getFallbackStatus() { return _activeFallback; }
function isModelNotFound(err) { /* status 404 + message matching */ }
async function callProvider(provider, model, ...) { /* was the body of callLLM */ }
async function callLLM(...) {
  const chain = [primary, ...FALLBACK_CHAINS[provider]];
  for (let i = 0; i < chain.length; i++) {
    try {
      const result = await callProvider(provider, chain[i], ...);
      if (i === 0) clearFallbackForProvider(provider);
      else setFallback(chain[0], chain[i]);
      return result;
    } catch (err) {
      if (isModelNotFound(err) && i < chain.length - 1) continue;
      throw err;
    }
  }
}
```

Config endpoint: `/api/config/api-keys` now includes `fallback: getFallbackStatus()`. Frontend reads it and renders an amber banner if populated.

### Test matrix (performed locally on computer 1 before any commit)

| Test | Expected | Result |
|---|---|---|
| Baseline: primary Gemini call (chat) | 200, answer, no fallback | ✅ `answer: "Yes, this wiki is designed to be a dedicated second brain..."` |
| `/api/config/api-keys` after baseline | `fallback: null` | ✅ |
| `LLM_MODEL=gemini-deprecated-nonexistent` override → chat | Fallback fires, answer returned | ✅ `answer: "FALLBACK OK"` |
| Server log during simulated deprecation | Two `[llm]` warnings (not-found + using-fallback) | ✅ Both present with clean text |
| `/api/config/api-keys` after fallback | `fallback: {provider, requestedModel, usingModel, at}` populated | ✅ All four fields correct |
| Ingest a tiny `.txt` with fallback active | 5 pages written, SSE stream completes cleanly | ✅ `pagesWritten: ["summaries/mini-fallback-test.md", 4 concepts]` |
| MCP self-test with fallback active | `ok: true`, 10 tools, all domains | ✅ |
| Health scan with fallback active | 597 brokenLinks, 0 missingBacklinks (v2.3.9 baseline) | ✅ |
| Sync status with fallback active | unchanged, returns current repo state | ✅ |
| Restart WITHOUT override → primary call | Primary succeeds, `fallback: null` | ✅ |
| Restart WITHOUT override → then any feature | No warnings in log, no UI banner | ✅ |
| Update-check endpoint | `current: 2.3.9, updateAvailable: false` (pre-bump) | ✅ |

### Anthropic pipeline verification

The user reports never having tested Anthropic in production. The code path was statically audited:

1. **Model ID in `DEFAULTS`:** `claude-sonnet-4-6`. The bundled SDK v0.39.0 `Model` type lists up to `claude-3-7-sonnet-latest` explicitly, but accepts arbitrary strings via `(string & {})` — so the newer ID will pass build and runtime *if Anthropic's API accepts it*. If it 404s, the fallback chain catches it with `claude-sonnet-4-5` → `claude-3-7-sonnet-latest` → `claude-3-5-sonnet-latest`.
2. **JSON response handling:** Anthropic has no `responseMimeType` equivalent. The `ingest.js` prompts instruct *"Return ONLY valid JSON"* explicitly; downstream, `parseJSON()` with `jsonrepair` absorbs minor deviations. Functionally equivalent but less deterministic than Gemini's native JSON mode.
3. **Error detection:** Anthropic SDK throws typed `NotFoundError` with `status: 404`. The `isModelNotFound()` helper catches this via the explicit `err.status === 404` branch.

Result: Anthropic pipeline is structurally sound; the one caveat is that JSON-producing calls (ingest) may occasionally require the `jsonrepair` safety net more often than with Gemini. This is noted in `docs/model-lifecycle.md`.

### Deliverables

- [`src/brain/llm.js`](../src/brain/llm.js) — fallback chain, `getFallbackStatus()`, `isModelNotFound()`
- [`src/routes/config.js`](../src/routes/config.js) — `/api/config/api-keys` grows a `fallback` field
- [`src/public/app.js`](../src/public/app.js) — `renderFallbackBanner()` attached to `loadApiKeyStatus()`
- [`src/public/styles.css`](../src/public/styles.css) — `.settings-fallback-banner` class
- [`docs/model-lifecycle.md`](model-lifecycle.md) — new user-facing guide
- [`CLAUDE.md`](../CLAUDE.md) — v2.4.0 row + active-decision entry
- [`package.json`](../package.json) — version 2.3.9 → 2.4.0

### Non-deliverables (explicitly out of scope for Phase 0)

- No AI-assisted Health fix endpoints (Phase 1+)
- No user-facing model picker in Settings (deferred; `LLM_MODEL` env var remains the developer escape hatch)
- No upgrade to newer Anthropic SDK version (noted as future work; v0.39.0 accepts string model IDs so functional)
- No changes to the ingest, chat, health-scan, health-fix, sync, MCP, updater, or installer code paths

### Rollback plan

The entire Phase 0 change set is one commit (v2.4.0). `git revert <sha> && git push` reverts cleanly because:
- No data format or schema changes
- No dependency additions
- No existing API endpoints modified (only field additions, which older clients ignore)
- No filesystem layout changes

### Next phase

Phase 1 — AI-suggest for broken links (`/api/health/:domain/ai-suggest`). Builds on top of v2.4.0's hardened LLM layer. Planned as a separate commit / version bump. Audited after Phase 1 work lands.

---

## Phase 0 Addendum — Anthropic low-cost default (v2.4.1)

### Motivation

v2.4.0 shipped with `DEFAULTS.anthropic = 'claude-sonnet-4-6'` — Sonnet being Anthropic's *mid-tier* model. For wiki ingestion of large libraries, this mis-matched the cost profile of the Gemini default (`gemini-2.5-flash-lite`, which is explicitly the low-cost tier). The user clarified that the Anthropic default should be the low-cost tier too.

### Change

- `DEFAULTS.anthropic`: `claude-sonnet-4-6` → **`claude-haiku-4-5`**
- `FALLBACK_CHAINS.anthropic` reordered to prefer Haiku variants first (same cost tier), then escalate to Sonnet family only if the entire Haiku line is retired:
  - `claude-3-5-haiku-latest` — previous Haiku gen, SDK-typed, definitely works
  - `claude-3-5-haiku-20241022` — explicit stable version as last-resort Haiku
  - `claude-sonnet-4-5` — quality upgrade tier
  - `claude-3-7-sonnet-latest` — rolling alias
  - `claude-3-5-sonnet-latest` — deep fallback

### Opt-out for users who want Sonnet

Documented in `docs/model-lifecycle.md`:

```bash
# in .env — if you want higher-quality Anthropic for dense content
LLM_MODEL=claude-sonnet-4-5
```

The fallback chain still applies on top of the override.

### Step 3 (fallback verification) completed on computer 1

Per user request, I executed the full Step 3 test matrix myself on computer 1. All checks passed:

| # | Action | Expected | Actual |
|---|---|---|---|
| 3a | Start with `LLM_MODEL=gemini-deprecated-nonexistent` | `activeModel` reports the bad name, `fallback: null` until first call | ✅ exactly as predicted |
| 3b | Trigger chat | Response arrives normally via fallback | ✅ answer: `"FALLBACK TEST SUCCESS"` |
| 3c | `/api/config/api-keys` after chat | `fallback: {provider, requestedModel, usingModel, at}` populated | ✅ `fallback.usingModel = "gemini-2.5-flash"` |
| 3d | Server log | Two `[llm]` warnings (not-found + using-fallback) | ✅ both present |
| 3e | Frontend code path | JS reads `data.fallback`, creates `#settings-model-fallback` element with class `settings-fallback-banner`, CSS defined | ✅ all three wiring points verified |
| 3f | Sync / Health / MCP under fallback | All succeed with identical output to baseline | ✅ sync status clean, health scan 597/0/794, MCP self-test ok + 10 tools |
| 3g | Restart without override | `fallback: null`, clean log, primary works | ✅ `answer: "Hey there! I've got the wiki ready..."` |

### Why ship as v2.4.1 rather than folding into v2.4.0

v2.4.0 was already pushed and verified by the user on computer 2. The Anthropic-default change is a **user-visible default** (changes cost profile) that warrants its own version row in the git-history table and deserves to be pulled separately so users on v2.4.0 can read the changelog before opting in.

### Deliverables

- [`src/brain/llm.js`](../src/brain/llm.js) — `DEFAULTS.anthropic` + `FALLBACK_CHAINS.anthropic` updated
- [`docs/model-lifecycle.md`](model-lifecycle.md) — Anthropic section reflects Haiku-first default + Sonnet opt-in
- [`CLAUDE.md`](../CLAUDE.md) — v2.4.1 row, version constant bumped to 2.4.1

---

## Phase 0 Addendum 2 — API-key UX fix (v2.4.2)

### Motivation

After v2.4.1 shipped, the user reported while testing provider switching on computer 2: *"which key has a priority because I cannot empty the field? … we should have the ability to disconnect from the connected LLM. … Nobody will use two same keys at the same time, this makes no sense."*

The v2.4.0/v2.4.1 behaviour had two concrete UX problems:

1. **No way to clear a stored key.** The Settings form's password inputs showed masked placeholders; typing nothing and clicking Save just kept the existing value (empty fields were filtered out of the POST body). Users had no control over which keys were stored.
2. **Hidden priority rule.** When both keys were stored, `getProviderInfo()` always preferred Gemini. To switch to Anthropic, a user had to open `.curator-config.json` in a text editor and manually delete the Gemini key — clearly not acceptable.

### Design

Two changes, both minimal and intuitive:

**A. Last-saved-wins.** Saving a non-empty key for a provider also marks that provider as active (`cfg.activeProvider`). Users don't juggle priority — they just paste the key they want to use. If a Gemini user wants to try Anthropic, they paste their Anthropic key and save. Done.

**B. Per-field Disconnect button.** For when you want to remove a key without adding another. Appears only when a key is stored. If the disconnected key was the active provider, active automatically moves to the other provider (if it has a key) or becomes null.

### Implementation

- [`src/brain/config.js`](../src/brain/config.js):
  - `setApiKeys({...})`: now also sets `cfg.activeProvider` to whichever non-empty key was just saved.
  - New `clearApiKey(provider)`: clears one provider's stored key and adjusts `activeProvider` if needed.
  - New `getActiveProvider()`: reads `cfg.activeProvider` with legacy fallback (Gemini-first-if-both) for configs written before v2.4.2.
- [`src/brain/llm.js`](../src/brain/llm.js):
  - `getProviderInfo()` uses `getActiveProvider()` instead of the hardcoded Gemini-first priority. A defensive fallback still exists if the active provider's key is somehow missing (e.g., manual config edit).
- [`src/routes/config.js`](../src/routes/config.js):
  - New endpoint `POST /api/config/api-keys/disconnect` with body `{provider}`.
- [`src/public/index.html`](../src/public/index.html) + [`src/public/app.js`](../src/public/app.js):
  - Each API-key field gains a small `Disconnect` button that appears only when the underlying key is stored.
  - `loadApiKeyStatus()` toggles the button's visibility from the `/api/config/api-keys` response.
  - A clickable confirmation prompt avoids accidental removal.
- [`src/public/styles.css`](../src/public/styles.css): `.key-field-footer`, `.key-disconnect-btn`, `.key-save-row`, `.key-save-hint` — minimal styling reusing existing design tokens.

### Backward compatibility

Existing `.curator-config.json` files don't have an `activeProvider` field. `getActiveProvider()` detects this and falls back to the v2.4.1 priority rule (whichever key exists; Gemini first if both), so existing users upgrading to v2.4.2 see zero behaviour change until they explicitly save a new key.

### Test matrix (computer 1)

Ran with a throwaway config to avoid touching the real keys:

| # | Action | Expected | Actual |
|---|---|---|---|
| 1 | Fresh config, no keys | `activeProvider: null`, badge hidden | ✅ |
| 2 | Save Gemini key | `activeProvider: gemini`, active model = `gemini-2.5-flash-lite` | ✅ |
| 3 | Save Anthropic key (both now stored) | `activeProvider: anthropic` (last-saved-wins), active model = `claude-haiku-4-5` | ✅ |
| 4 | Re-save Gemini with a different key | `activeProvider: gemini` again | ✅ |
| 5 | Disconnect Gemini (was active) | `activeProvider: anthropic` auto-switched (Anthropic key still stored) | ✅ |
| 6 | Disconnect Anthropic (the only remaining) | `activeProvider: null`, no active model | ✅ |
| 7 | Legacy config (no `activeProvider` field) + real chat | Works with Gemini-first fallback, `answer: "LEGACY COMPAT OK!"` | ✅ |

### Rollback

One commit, strictly additive at the config/routes/UI level. No data migration. Reverting restores v2.4.1 behaviour cleanly.


---

## Addendum — v2.4.3 (AI Wiki Health, Phase 1)

### Scope

Phase 1 of the three-phase AI Wiki Health roadmap: surface an **✨ Ask AI** button on broken-link rows where the algorithmic resolver returned `suggestedTarget: null`. Users can ask the LLM to propose the intended target and apply the suggestion through the existing fix pipeline.

### What was audited

- [src/brain/health.js](../src/brain/health.js) — confirmed `scanWiki` already emits `brokenLinks` with a nullable `suggestedTarget`; confirmed `fixBrokenLink` performs the rewrite from `issue.suggestedTarget` regardless of origin. No changes needed.
- [src/routes/health.js](../src/routes/health.js) — existing `/fix` endpoint is reused untouched for applying AI suggestions.
- [src/brain/llm.js](../src/brain/llm.js) — `generateText()` + `getProviderInfo()` confirmed provider-agnostic; the v2.4.0 fallback chain applies automatically to every AI Health call.
- [src/public/app.js](../src/public/app.js) `renderSection()` — extended the review-only branch for `brokenLinks` to inject a second button; no existing control paths were modified.

### What was added

| File | Change |
|---|---|
| `src/brain/health-ai.js` (new, 179 lines) | Module with one exported function `suggestBrokenLinkTarget(domain, issue)`. Strictly read-only. Hallucinated slugs are validated against the on-disk slug set and coerced to `null` before leaving the server. |
| `src/routes/health.js` | Added `GET /ai-available` (probe for configured API key) and `POST /:domain/ai-suggest` (Phase 1 accepts only `type: 'brokenLinks'`). |
| `src/public/app.js` | `checkAiAvailable()` session cache; `✨ Ask AI` button rendered when a broken-link row has no `suggestedTarget` AND AI is available; `runAiSuggest()` inline result block; one-time `ensureAiDisclosure()` modal gated on localStorage key `curator-ai-health-disclosure-seen-v1`. Apply button reuses `fixOne()` with `issue.suggestedTarget = result.target`. |
| `src/public/index.html` | One new overlay: `#ai-health-disclosure`, styled as the existing onboarding overlay. |
| `src/public/styles.css` | `.health-ai-*` classes — button, inline result block, confidence chips (high / medium / low). |
| `docs/ai-health.md` (new) | User-facing guide: what AI adds, privacy, cost, how to disable, developer notes. |
| `CLAUDE.md` | New v2.4.3 git-history row + new active-decisions bullet: "AI Health is READ-ONLY". |
| `package.json` | 2.4.2 → 2.4.3. |

### Invariants preserved

- **No new write paths.** Applying an AI suggestion calls the same `POST /api/health/:domain/fix` endpoint, same `fixBrokenLink()` function, same regex-based rewrite — with `issue.suggestedTarget` supplied by AI instead of the algorithmic matcher.
- **Hallucination guard.** `suggestBrokenLinkTarget` validates the AI's target against `new Set([...entitySlugs, ...conceptSlugs, ...summarySlugs.map(s => 'summaries/' + s)])` before returning. Unknown slugs become `target: null, confidence: 'low'`.
- **Privacy disclosure.** Shown once, ever, per browser. No AI call is made until the user has clicked Continue.
- **No API key = no button.** `GET /api/health/ai-available` returns `{available: false}` if `getProviderInfo()` throws; the frontend caches that per-scan and renders the row as plain Review.
- **Provider-agnostic.** No hardcoded Gemini/Anthropic branches in `health-ai.js` — dispatch goes through `generateText()`.

### Test matrix (Computer 1)

Verified on Computer 1 before push. Backend via curl, UI via The Curator.app.

| # | Action | Expected | Actual |
|---|---|---|---|
| 1 | Boot `.app` at v2.4.3 | Server starts, Gemini active, `/api/health/ai-available` returns `{available: true}` | ✅ |
| 2 | Scan `articles` domain (2313 pages, 619 broken links) | Report returns in ~1s; ✨ Ask AI button visible on review-only broken-link rows | ✅ |
| 3 | Click ✨ Ask AI on `concepts/actionable-intelligence.md → [[data-analysis]]` | Disclosure modal shown; accepted → LLM hallucinated a non-existent slug; server guard coerced to `target: null, confidence: low`; UI showed "🤖 No good target · low confidence" with no Apply button | ✅ |
| 4 | Click ✨ Ask AI on a second row | No disclosure (localStorage remembered); `[[deployment-targets]]` → `deployment-architecture`, medium confidence, Apply+Skip shown | ✅ |
| 5 | Click ✨ Ask AI on a third row | `[[human-vs-machine-intelligence-comparison.md]]` (with bogus `.md` suffix) → `human-vs-machine-intelligence-comparison`, high confidence | ✅ |
| 6 | Click Apply on a suggestion | Existing `fixOne()` used; rescan shows one less broken link | ✅ |
| 7 | Existing algorithmic Fix buttons (missing backlinks, hyphen variants, cross-folder) | Still work unchanged | ✅ |
| 8 | Ingest a small file | No regression | ✅ (user-verified) |
| 9 | Chat against `articles` | No regression | ✅ (user-verified) |
| 10 | MCP self-test | Passes, 10 tools listed, 3 domains | ✅ |
| 11 | `/api/version`, `/api/domains`, `/api/config/api-keys` | No regression | ✅ |
| 12 | Unsupported type (`orphans`) via ai-suggest | HTTP 400 with clean error | ✅ |

### Rollback

One commit, strictly additive. `git revert <sha> && git push`. Next Check-for-Updates on end-user machines reverts them cleanly. No data migration required — the only persistent state added is a `localStorage` key in the user's browser, which is harmless if left over.


---

## Addendum — v2.4.4 (AI Wiki Health, Phase 2: Orphan rescue)

### Scope

Phase 2 of the three-phase roadmap: surface an **✨ Ask AI** button on orphan rows. Clicking it returns up to 5 existing entity/concept pages that should link to the orphan, each with an AI-written description. Applying one writes `- [[orphanSlug]] — description` into the target's `## Related` section, which rescues the orphan on the next scan.

### What was audited

- [src/brain/health.js](../src/brain/health.js) `scanWiki` — confirmed `orphans` already emitted as `{path, type, slug}`; no change to the scanner.
- [src/brain/health.js](../src/brain/health.js) `fixIssue` switch — the new `orphanLink` pseudo-type slots in cleanly; `fix-all` is a no-op for `orphanLink` (scanner never emits it).
- [src/brain/files.js](../src/brain/files.js) — `injectSingleBacklink` hardcodes the `summaries/` prefix, so a sibling helper was needed rather than generalising the existing one (preserves the summary-backlink invariant used by v2.3.9 and the ingest pipeline).
- [src/routes/health.js](../src/routes/health.js) — the `/ai-suggest` endpoint already type-dispatches; adding `orphans` was a two-line change.
- [src/public/app.js](../src/public/app.js) `renderSection` — the review-only branch already supports an alternate-button injection pattern from Phase 1; extended for orphans with zero structural change.

### What was added

| File | Change |
|---|---|
| `src/brain/files.js` | New export `injectRelatedLink(targetFilePath, linkSlug, description)` — dedup-safe generic `- [[slug]] — description` injection into a target's `## Related` section. Mirrors `injectSingleBacklink` but without the hardcoded `summaries/` prefix. |
| `src/brain/health.js` | Added `'orphanLink'` to `AUTO_FIXABLE`. New `fixOrphanLink(wikiDir, issue)` applies four defences (slug regex, orphan-on-disk, target-in-entities-or-concepts, self-link rejection) before calling `injectRelatedLink`. Fix-all short-circuits to `{fixed: 0, total: 0}` for `orphanLink` (scanner never emits it). |
| `src/brain/health-ai.js` | New export `suggestOrphanHomes(domain, issue)` — builds an entity+concept-only slug inventory (summaries excluded), reads up to 4 KB of the orphan body, calls `generateText` with a JSON-response prompt asking for 1–5 candidates, then validates every target against on-disk slugs. Clamps to 5; trims descriptions to 140 chars; coerces unknown confidences to `low`. |
| `src/routes/health.js` | `/ai-suggest` accepts `type: 'orphans'` in addition to `brokenLinks`. Response shape differs per type (`{target,rationale,confidence}` vs `{candidates:[...]}`). |
| `src/public/app.js` | `renderSection` renders ✨ Ask AI on orphan rows when AI is available. `runAiSuggest` type-dispatches to `renderBrokenLinkAiResult` or new `renderOrphanAiResult`, which renders up to 5 candidate cards with per-candidate Apply/Skip. Apply calls the existing `/fix` endpoint with `type: 'orphanLink'` and the target slug — server resolves the path. |
| `src/public/styles.css` | `.health-ai-candidates` (vertical stack) + `.health-ai-candidate` (mini-card) + `.health-ai-candidate-desc` (highlighted description preview) + `.health-ai-candidate-actions`. |
| `docs/ai-health.md` | New "How it works (Phase 2 — orphan rescue)" section, updated cost block, new design note explaining why summaries are excluded as candidate targets, endpoint table updated. |
| `CLAUDE.md` | New v2.4.4 git-history row; version bumped. |
| `package.json` | 2.4.3 → 2.4.4. |

### Invariants preserved

- **AI Health is READ-ONLY.** `suggestOrphanHomes` does not touch the filesystem. All writes go through `fixIssue` → `fixOrphanLink` → `injectRelatedLink` — same one-write-path rule as Phase 1.
- **Hallucination guard.** AI candidates are filtered against the on-disk entity/concept slug set before reaching the UI. Unknown, self-referencing, and duplicate targets are dropped.
- **Summaries can never be orphan-rescue targets.** Enforced in three places: (1) the candidate inventory sent to the LLM excludes summary slugs; (2) the server-side validator only accepts targets that exist in `entities/` or `concepts/`; (3) `fixOrphanLink` explicitly checks the resolved target folder.
- **No re-prompt for privacy disclosure.** Phase 1's localStorage key covers Phase 2 — same data shape (excerpt + slug list) leaves the machine.

### Test matrix (Computer 1)

Backend-only (UI still to be verified by user).

| # | Action | Expected | Actual |
|---|---|---|---|
| 1 | Boot `.app` at v2.4.4 | Server starts, Gemini active, existing endpoints work | ✅ |
| 2 | `POST /api/health/articles/ai-suggest` with `{type:'orphans', issue}` | Returns up to 5 candidates, each with target+description+confidence+rationale | ✅ (5 candidates returned for `acl-findings`) |
| 3 | `POST /api/health/articles/fix` with `{type:'orphanLink', issue:{orphanSlug, targetSlug, description}}` | Target's `## Related` gains `- [[orphanSlug]] — description`; response `{ok:true, fixed:1}` | ✅ |
| 4 | Re-scan after step 3 | Orphan no longer in the orphans list | ✅ |
| 5 | Re-apply same fix | Dedup prevents a second bullet; response `{ok:true, fixed:0}` | ✅ |
| 6 | Apply with non-existent `orphanSlug` | Response `{ok:true, fixed:0}` — defence held | ✅ |
| 7 | Apply with path-traversal `orphanSlug` (e.g. `../../etc`) | Response `{ok:true, fixed:0}` — slug regex rejected | ✅ |
| 8 | Apply with summary-only `targetSlug` | Response `{ok:true, fixed:0}`; summary file untouched (mtime verified) | ✅ |
| 9 | Existing algorithmic Health fixes | Unchanged | ✅ |
| 10 | Phase 1 Ask AI flow (broken links) | Unchanged | ✅ |

### Notes for manual UI verification

- The backend test added `- [[acl-findings]] — ACL Findings is a venue publishing AI research.` to `entities/artificial-intelligence-research.md` in the `articles` domain. That is a correct real-world edit; no revert needed.
- On Computer 1 UI test: the `acl-findings` orphan may already be rescued and no longer appear in the orphans list. Pick any other orphan to test.

### Rollback

One commit, strictly additive. `git revert <sha> && git push`. No data migration. Reverting leaves any user-applied orphan bullets in place (they are valid wiki content on their own) — which is the correct behaviour; data changes made during the feature's lifetime are not undone by removing the feature.


---

## Addendum — v2.4.5 (AI Wiki Health, Phase 3: Semantic near-duplicates)

### Scope

Phase 3 of the roadmap: detect and merge pages that describe the same concept under different slugs — duplicates the existing algorithmic scanner cannot see (e.g. `email` / `e-mail`, `rag` / `retrieval-augmented-generation`, `neural-network` / `neural-networks`). First feature in The Curator that deletes user-authored files; shipped behind a mandatory Preview-diff safety gate.

### Scale evaluation before building

Documented as part of the planning, reproduced here:

| Stage | Current domain (~2.4k pages) | Projected 10k | Projected 50k |
|---|---|---|---|
| Pre-filter | <1s | 1-2s | 5-10s |
| Candidate pairs after filter | 200-500 | 800-2000 | 3000-8000 |
| LLM cost (default cap 500) | ~$0.015 | ~$0.015 | ~$0.015 (capped) |
| Merge (per pair) | <5s | <15s | <60s |

Hard caps in code: `SEMANTIC_DUPE_MAX_DOMAIN_PAGES = 20000`; default pair cap 500; default cost ceiling 50k tokens. **No batch merge at any scale.**

### What was added

| File | Change |
|---|---|
| `src/brain/health.js` | `findSemanticCandidatePairs()` (inverted-token index, O(N·k)), `countLinksToSlug`, `previewSemanticDuplicateMerge`, `fixSemanticDuplicate` (merge + domain-wide link rewrite + delete). New pseudo-fix-type `semanticDupe` in `AUTO_FIXABLE`. |
| `src/brain/health-ai.js` | `estimateSemanticDuplicateScan(domain, maxPairs)`, `scanSemanticDuplicates(domain, opts, onEvent)` (yields SSE-ready events). Batching of 20 pairs per LLM call; each pair sends slug + 500-char first paragraph of each page. |
| `src/brain/config.js` | `getAiHealthSettings` / `setAiHealthSettings` persisted under `aiHealth` key in `.curator-config.json`. |
| `src/routes/health.js` | 5 new endpoints: `GET/POST /ai-settings`, `GET /:domain/semantic-dupes/estimate`, `POST /:domain/semantic-dupes/scan` (SSE), `POST /:domain/semantic-dupes/preview`. Route order fixed so `ai-settings`/`ai-available` don't collide with `/:domain`. |
| `src/public/index.html` | Semantic-duplicates section in the Health tab; confirm modal with cost estimate; merge-preview modal with affected-files list + merged-content sample. Settings tab gained an AI Wiki Health section with cost ceiling + candidate-pair cap inputs. |
| `src/public/app.js` | `runSemanticScan` + SSE reader; `renderSemanticPairCard`; `openMergePreview` (marks the pair as "previewed" — the Merge button stays disabled until this runs). Settings tab handlers `loadAiHealthSettings` + save. |
| `src/public/styles.css` | Pair cards, confidence chips (reuse Phase 1/2 styles), progress bar, preview modal layouts. |
| `docs/ai-health.md` | Full Phase 3 section — pipeline, scale caps, cost, what it won't do. Privacy disclosure extended with the per-pair 500-char sample. |
| `docs/api-reference.md` | 5 new endpoints + `semanticDupe` fix type + SSE event schema. |
| `docs/architecture.md` | Module line + dispatch block updated for `semanticDupe`. |
| `docs/user-guide.md` | New "Semantic duplicates" subsection in the Health section. |
| `docs/model-lifecycle.md` | Scope line extended. |
| `CLAUDE.md` | New v2.4.5 git-history row; two new active-decisions entries (opt-in/cost-gated scan; destructive-merge safety gate); version bumped. |
| `package.json` | 2.4.4 → 2.4.5. |

### Invariants preserved

- **AI Health remains READ-ONLY at the suggestion layer.** `scanSemanticDuplicates` never writes. All merges flow through `POST /api/health/:domain/fix` with `type: 'semanticDupe'` → `fixSemanticDuplicate`.
- **No batch merge at any scale.** `fixIssue` short-circuits to `{fixed: 0, total: 0}` when called with `type: 'semanticDupe'` and no issue (the "fix-all" path).
- **Four defences before any write:** slug regex (`^[a-z0-9][a-z0-9.-]*$/i`), keep/remove folders must be entities or concepts (never summaries), both files must exist, not the same slug-folder pair.
- **Destructive-merge safety gate.** Preview-diff must be opened for a pair before its Merge button enables. Frontend tracks this in `_semPreviewedPairs` — fresh sessions require a fresh preview.

### Test matrix (Computer 1, backend)

| # | Action | Expected | Actual |
|---|---|---|---|
| 1 | Boot `.app` at v2.4.5 | Server starts, Gemini active | ✅ |
| 2 | `GET /ai-settings` (fresh config) | `{costCeilingTokens:50000, semanticDupeMaxPairs:500}` | ✅ |
| 3 | `POST /ai-settings {costCeilingTokens:300000}` | Persisted; GET returns updated value | ✅ |
| 4 | `GET /:domain/semantic-dupes/estimate` on `articles` (2434 pages) | Returns 500-pair cap with truncated:true, 18152 total candidates, 200k tokens estimated, Flash Lite pricing | ✅ |
| 5 | `POST /:domain/semantic-dupes/scan` over SSE with cap=10 | Received start, 7 pair events (all medium/high confidence, including `email`↔`e-mail`, `nextjs`↔`next.js`, `mountvacation`↔`mountvacation.com`, `payroll`↔`payrolls`), progress, done events | ✅ |
| 6 | `POST /:domain/semantic-dupes/preview {email, e-mail}` | Returns affectedFiles=3, totalLinksRewritten=3, mergedLength=1836, cap at 50 entries | ✅ |
| 7 | `POST /fix {type:semanticDupe, issue:{payrolls→payroll}}` | payrolls.md deleted, 0 `[[payrolls]]` references remain, payroll.md grew from 366B to 1188B with merged sections + Related section enriched | ✅ |
| 8 | Defence: `keepFolder: summaries` | `{fixed:0}`, no write | ✅ |
| 9 | Defence: same slug + same folder | `{fixed:0}` | ✅ |
| 10 | Defence: slug regex (`../etc`) | `{fixed:0}` | ✅ |

### Notes for manual UI verification

- Backend real-merged `payrolls` → `payroll` during testing. That pair will not reappear in scans on Computer 1. Other detected pairs (`email`/`e-mail`, `nextjs`/`next.js`, `mountvacation`/`mountvacation.com`, `ai-and-epidemic-prediction`/`ai-for-epidemic-prediction`, `centralized-vs-decentralized`/`centralized-vs-decentralized-ai`) remain available for UI test.
- After a merge, the Health main scan should auto-re-run and show one fewer concept/entity in the counts.

### Rollback

One commit, strictly additive. `git revert <sha> && git push`. Non-destructive to user data: merges already applied stay applied (they're valid wiki content). Removing the code disables the feature but doesn't resurrect deleted pages.
