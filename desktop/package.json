{
  "name": "the-curator-desktop",
  "productName": "The Curator",
  "version": "0.0.0",
  "private": true,
  "description": "Electron shell for The Curator. SCAFFOLD ONLY — never installed, never built. See desktop/README.md.",
  "license": "MIT",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --mac --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0"
  }
}
